export enum Comparator {
    Equal = '==',
    NonEqual = '!=',
    Bigger = '>',
    BiggerOrEqual = '>=',
    Smaller = '<',
    SmallerOrEqual = '<=',
    Contains = 'contains',
    NotContains = 'notContains',
}
